; ##########################################
; ### clock
; ##########################################

(defwidget bar_clock []
  (button
    :onclick "eww open calendarmenu"
    :class "clock-button"
    (label
      :text {formattime(EWW_TIME, "%B %d  %H:%M" )}
    )
  )
)
